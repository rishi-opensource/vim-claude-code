" test/vimrc — minimal vimrc for running Vader tests
set nocompatible

" Point runtimepath to this plugin root
let &rtp = expand('<sfile>:p:h:h') . ',' . &rtp

" Load Vader from $VADER_PATH if set
if !empty($VADER_PATH)
  let &rtp .= ',' . $VADER_PATH
endif

" Source the plugin
runtime plugin/claude_code.vim

" Force-load all autoload modules so exists('*func') checks work in tests.
" Autoload functions are not registered until their file is first sourced.
runtime autoload/claude_code/util.vim
runtime autoload/claude_code/config.vim
runtime autoload/claude_code/git.vim
runtime autoload/claude_code/terminal.vim
runtime autoload/claude_code/meta_commands.vim
runtime autoload/claude_code/git_commands.vim
runtime autoload/claude_code/workflow_commands.vim
